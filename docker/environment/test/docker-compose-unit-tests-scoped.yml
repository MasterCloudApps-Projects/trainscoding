version: "3.9"

services:

  unit-tests:
    image: javiergarciagon/trainscoding-unit-tests:latest
    environment:
      SCOPE: ${SCOPE}
    volumes:
      - ../../../:/trainscoding-test
    command: |
      - npm ci
      - npm t -- --projects ${SCOPE}
